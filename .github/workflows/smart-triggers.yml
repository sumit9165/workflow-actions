name: Smart Trigger Workflow

on:
  push:
    branches:
      - main
      - release/*
    paths:
      - 'src/**'
      - 'app/**'

jobs:
  run-if-app-code-changed:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger confirmation
        run: echo "Code change detected in src/ or app/"